<h2 style="margin-bottom: 20px">
    <strong>What Is Sport Log?</strong>
</h2>
<div style="margin: 0 25px; padding-bottom: 20px">
    <h4>
        Sport Log is a place for all people who like to move!
    </h4>
    <div style="overflow-x: auto;">
        <div class="homepage-icon-wrapper">
            <div class="col-xs-3 centered homepage-icon">
                <p><strong>Choose Challenge</strong></p>
                {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                <a href="{{ path('challenges') }}">
                    {% endif %}
                    <img src="{{ asset('assets/images/challenges.png') }}" height="150" width="150" onerror="imageError(this)"
                         alt="challenges" class="img-circle {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %} my-img-style {% else %} my-img-style-static
                            {% endif %}">
                    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                </a>
                {% endif %}
            </div>
            <div class="col-xs-3 centered homepage-icon">
                <div class="col-xs-2 plus-padding-fix">
                    <h1 style="display: inline"><strong>+</strong></h1>
                </div>
                <div>
                    <p><strong>Choose Sport</strong></p>
                    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                    <a href="{{ path('sports') }}">
                        {% endif %}
                        <img src="{{ asset('assets/images/sports.png') }}" height="150" width="150" onerror="imageError(this)"
                             alt="sports" class="img-circle {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %} my-img-style {% else %} my-img-style-static
                                {% endif %}">
                        {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                    </a>
                    {% endif %}
                </div>
            </div>
            <div class="col-xs-3 centered homepage-icon">
                <div class="col-xs-2 arrow-padding-fix">
                    <h1 style="display: inline"><strong>🡪</strong></h1>
                </div>
                <div>
                    <p><strong>Log It</strong></p>
                    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                    <a href="{{ path('sport_logs') }}">
                        {% endif %}
                        <img src="{{ asset('assets/images/log.png') }}" height="150" width="150" onerror="imageError(this)"
                             alt="log" class="img-circle {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %} my-img-style {% else %} my-img-style-static
                                {% endif %}">
                        {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                    </a>
                    {%  endif %}
                </div>
            </div>
            <div class="col-xs-3 centered homepage-icon">
                <div class="col-xs-2 arrow-padding-fix">
                    <h1 style="display: inline"><strong>🡪</strong></h1>
                </div>
                <div>
                    <p><strong>Compete With Friends</strong></p>
                    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                    <a href="{{ path('groups') }}">
                        {% endif %}
                        <img src="{{ asset('assets/images/groups.png') }}" height="150" width="150" onerror="imageError(this)"
                             alt="groups" class="img-circle {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %} my-img-style {% else %} my-img-style-static
                                {% endif %}">
                        {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                    </a>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
</div>